Place `hand_landmarker.task` here — see the module's top-level README.md
"Setup" section for the download command. Not committed to git (binary,
~7-30MB depending on model variant); each dev/CI machine fetches it once.
